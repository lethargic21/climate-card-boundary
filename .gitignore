# 비밀값 - 절대 커밋 금지
.env
.env.*
!.env.example

# 원본 데이터 - 수정 금지, 재현은 src/01_fetch_ridership.py로
data/raw/

# 파생 데이터(data/processed, 약 3MB)는 재현 편의를 위해 포함한다 - 다시 만들려면 03 → 02 → 06 types

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.pytest_cache/

# R
.Rhistory
.RData
.Rproj.user/

# OS / 편집기 / OneDrive
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.tmp
~$*

# 보고서 빌드 중간물(report/build_pdf.py가 다시 만든다)
report/*.html
report/_body_only.*
